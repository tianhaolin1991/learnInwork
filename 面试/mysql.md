### 事务
- 1.事务的隔离级别
```text
1.读未提交:可能引起脏读
2.读已提交:可以避免脏读,但是可能出现不可重复读
3.可重复读:可以避免不可重复读,但是可能会出现幻读 => 其实mysql是可以解决部分幻读的
    快照读通过:MVCC版本控制+undoLog来实现
    当前读:通过行级锁和
4.串行化:所有的事务都串行执行,可以避免幻读,但是由于效率问题基本不可用

脏读:读到未提交的数据
不可重复读: 在同一个事务中读取的数据不一致
幻读: 读取到新插入或者删除的行
```